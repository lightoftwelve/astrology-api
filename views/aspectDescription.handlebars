<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aspect Description</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            margin: 50px auto;
            background-color: #fff;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .celestial-images {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
        }

        .celestial-images img {
            width: 80px;
            height: auto;
            border-radius: 50%;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .description {
            margin-top: 30px;
            font-size: 1.1em;
            line-height: 1.6em;
        }

        .back-btn {
            display: block;
            margin: 30px auto;
            padding: 10px 20px;
            background-color: #333;
            color: #fff;
            text-align: center;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }

        .back-btn:hover {
            background-color: #555;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="header">
            <h2>{{body_id_1}} & {{body_id_2}} - {{aspect}}</h2>
        </div>

        {{!-- <div class="celestial-images">
            <img src="/images/celestial_bodies/{{body_name_1}}.png" alt="{{body_name_1}}">
            <img src="/images/aspects/{{aspect}}.svg" alt="{{aspect}}" style="width: 50px;">
            <img src="/images/celestial_bodies/{{body_name_2}}.png" alt="{{body_name_2}}">
            <img src="/images/celestial_bodies/{{body_name_2}}.png" alt="{{body_name_2}}">
        </div> --}}

        <div class="description">
            <p>{{description}}</p>
        </div>

        <a href="/astrology/explore-astrology-aspects" class="back-btn">Go Back</a>
    </div>

</body>

</html>